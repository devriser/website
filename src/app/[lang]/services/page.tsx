import React from "react";
import MainServiceContent from "./MainServiceContent";

const MainService = ({ params }: any) => {
  return <MainServiceContent params={params} />;
};

export default MainService;
