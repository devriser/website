export type BlogEditorTypes = {
  title: string;
  summary: string;
  content: string;
  website: number;
  topics: string[];
  bannerImage: string;
};

export type AddTopicTypes = {
  topic: string;
};
